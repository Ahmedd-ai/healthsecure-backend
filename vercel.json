{
  "build": {
    "command": "CI=false npm run build",
    "outputDirectory": "src/build",
    "env": {
      "CI": "false"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/src/build/index.html"
    }
  ]
}
